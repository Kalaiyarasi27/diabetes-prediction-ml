# Diabetes Prediction — Modern UI

Short description: Streamlit app for diabetes risk prediction with PDF report and metrics.

## Run locally
1. Create virtualenv and activate:
   ```powershell
   python -m venv .venv
   .\.venv\Scripts\Activate.ps1
